require('dotenv').config({path: '.env.local'}); const fetch = require('node-fetch'); async function run() { const res = await fetch(process.env.KV_REST_API_URL + '/get/steam_items_queue', { headers: { Authorization: 'Bearer ' + process.env.KV_REST_API_TOKEN } }); const data = await res.json(); const q = JSON.parse(data.result || '[]'); console.log('Items remaining in Queue:', q.length); } run();
